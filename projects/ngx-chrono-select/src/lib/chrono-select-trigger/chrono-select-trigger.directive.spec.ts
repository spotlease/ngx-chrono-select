import { ChronoSelectTriggerDirective } from './chrono-select-trigger.directive';

describe('ChronoSelectTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ChronoSelectTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
