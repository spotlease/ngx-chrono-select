import { NgxChronoSelectTriggerDirective } from './trigger.directive';

describe('ChronoSelectTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxChronoSelectTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
